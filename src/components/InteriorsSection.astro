<section class="w-full py-section">
  <div class="max-w-[1240px] mx-auto flex flex-col gap-24">
    <!-- Row 1 -->
    <div class="flex flex-col md:flex-row items-center gap-20">
      <!-- LEFT: TEXT -->
      <div class="w-full md:w-1/2">
        <h3 class="text-[35px] font-light mb-8 fade-in-element opacity-0">
          Interiors
        </h3>

        <div class="space-y-6 text-[18px] font-light fade-in-element opacity-0">
          <p>
            Our services range from colour consultancy and space planning to
            full-home architectural and interior design.
          </p>
          <p>
            We develops strong relationships with clients to understand their
            needs, creating spaces that ease and enhance their daily lives.
          </p>
        </div>

        <a
          href="/projects"
          class="inline-block mt-10 border border-black bg-[#301804] text-[#EEECE3] rounded-full hover:bg-[#EEECE3] hover:text-[#301804] transition-colors duration-300 px-8 py-3 text-[18px] tracking-wide fade-in-element opacity-0"
        >
          View Projects
        </a>
      </div>

      <!-- RIGHT: IMAGE -->
      <div class="w-full md:w-1/2 fade-in-element opacity-0">
        <img
          src="/images/interiors-side.jpg"
          alt="Inscape Interiors Kensal Rise Interiors"
          class="w-full h-auto"
        />
      </div>
    </div>

    <!-- Row 2 (Wide image aligned to container) -->
    <div>
      <img
        src="/images/interiors-wide.jpg"
        alt="Inscape Interiors Interiors"
        class="w-full h-auto"
      />
    </div>
  </div>
</section>

<style>
  .fade-in-element {
    transition:
      opacity 0.8s ease-out,
      transform 0.8s ease-out;
    transform: translateY(20px);
  }

  .fade-in-element.visible {
    opacity: 1 !important;
    transform: translateY(0);
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry, index) => {
        if (entry.isIntersecting) {
          setTimeout(() => {
            entry.target.classList.add("visible");
          }, index * 600); // 200ms stagger delay
        }
      });
    },
    { threshold: 0.2 },
  );

  document.querySelectorAll(".fade-in-element").forEach((el) => {
    observer.observe(el);
  });
</script>
